import React from 'react';
import { Paper, Typography } from '@mui/material';

const StressRecorder = () => (
  <Paper elevation={3} sx={{ p: 2 }}>
    <Typography variant="h6">Progress of Well-being</Typography>
    {/* Add bar chart here */}
  </Paper>
);

export default StressRecorder;
